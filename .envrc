source activate ose-course

export COURSE_ROOT=$PWD

export PATH=$PATH:$COURSE_ROOT/bin

# We just use the configuration files from the analysis repository.
export MATPLOTLIBRC=$COURSE_ROOT/configurations
export JUPYTER_CONFIG_DIR=$COURSE_ROOT/configurations/jupyter
export IPYTHONDIR=$COURSE_ROOT/configurations/ipython
